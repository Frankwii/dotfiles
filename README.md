# Frank William's dotfiles
Current OS: Arch Linux

This repo keeps track of my configuration files for some programs, including NeoVim and Zathura.
Git tracking should be managed via the (executable) Bash script `git-addall.sh`.
